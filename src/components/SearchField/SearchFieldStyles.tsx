import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  textField: {
    borderColor: 'grey',
    padding: 10,
    margin: 10,
    borderRadius: 5,
    borderWidth: 1,
  },
});
